import type { DatatableColumnType } from "src/common/types";

export const COLUMN_TYPES: DatatableColumnType[] = ["text", "number", "boolean", "datetime", "select", "json"];

export const PAGE_SIZE = 50;
